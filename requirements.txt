# Dependências principais da pipeline.
#
# As faixas evitam atualizações de versão principal sem impedir correções
# compatíveis. A variante CPU/CUDA do PyTorch deve ser escolhida pelo índice
# do Pip e pelos módulos disponíveis no ambiente local ou no Santos Dumont.

numpy>=2.0,<3.0
pandas>=2.2,<3.0
PyYAML>=6.0,<7.0
scikit-learn>=1.5,<2.0

torch>=2.1,<3.0
transformers>=4.48,<6.0
safetensors>=0.4,<1.0
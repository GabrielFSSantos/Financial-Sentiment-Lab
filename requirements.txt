# Dependências principais da pipeline (conveniência para pip install manual).
#
# O setup recomendado é ./scripts/setup_env.sh, que instala requirements-base.txt
# e escolhe a variante CPU/CUDA do PyTorch automaticamente.

-r requirements-base.txt
torch>=2.1,<3.0
